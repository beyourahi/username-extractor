# Decisions

This document closes the open questions from the PRD and records the current non-functional posture. It is the gap-traceability artifact referenced by the completion task.

## Open question resolutions

| Q                                                | Resolution                                                                                                                         | Evidence                                                                                                                                                                                                                    |
| ------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Q1 Visual identity**                           | **Forensics** confirmed. Polish-only pass; no rebuild.                                                                             | `src/routes/+layout.svelte` (monospace chrome, "forensics build · v0.1" tag), `src/app.css` (color tokens).                                                                                                                 |
| **Q2 shadcn-svelte vs raw bits-ui**              | **Raw bits-ui** (current state). No shadcn-svelte layer added.                                                                     | `package.json` dependencies — `bits-ui` present, no `shadcn-svelte` entry; component implementations under `src/lib/components/` use bits-ui primitives directly (see `RawResponseDialog.svelte`, `FirstRunWizard.svelte`). |
| **Q3 Cloudflare Access tenancy**                 | **Single Access policy with private allowlist**.                                                                                   | `wrangler.jsonc` `ACCESS_TEAM_DOMAIN = "beyourahi.cloudflareaccess.com"`; verification logic in `src/lib/server/access.ts:verifyAccessJwt`; dev-only fallback gated by `ALLOW_DEV_AUTH=1`.                                  |
| **Q4 Encryption-at-rest scheme**                 | **AES-GCM with a Worker Secret as the key**.                                                                                       | `src/lib/server/crypto.ts:deriveTokenKey` (32-byte base64 key), `encryptNotionToken`, `decryptNotionToken`. Storage column: `user_settings.notion_token_encrypted` (BLOB) in `src/lib/server/schema.ts`.                    |
| **Q5 R2 expiration policy**                      | **30 days raw / 7 days debug**.                                                                                                    | `src/lib/server/cron/sweep.ts:RAW_RETENTION_MS` and `DEBUG_RETENTION_MS`; cron trigger in `wrangler.jsonc` (`"triggers": { "crons": ["0 3 * * *"] }`).                                                                      |
| **Q6 Benchmark protocol for Kimi K2.6**          | **Checked-in fixtures + `bun run benchmark` script + generated `docs/benchmark.md`. Not in CI** (Workers AI cost opt-in only).     | `scripts/benchmark.ts`, `src/lib/extract/__tests__/fixtures/expected.json`, `src/lib/extract/__tests__/fixtures/screenshots/*.png` (58 entries lifted from legacy `needs_review.md`).                                       |
| **Q7 Deployment domain**                         | **`https://username-extractor.beyourahi.workers.dev`** for v1, matching `invoice-generator` pattern. Subdomain migration deferred. | tool-card entries at `~/Desktop/projects/beyourahi.com/src/lib/content/data/tools.ts` and `~/Desktop/projects/dropout-studio/src/lib/content/data/tools.ts`.                                                                |
| **Q8 Legacy-import timing**                      | **Shipped in v1** as a UI section on `/settings`.                                                                                  | `src/routes/settings/+page.svelte` (legacy-import section) + `?/importLegacy` action in `src/routes/settings/+page.server.ts`, backed by `src/routes/api/import/legacy/+server.ts`.                                         |
| **Q9 (sub-decision) Pre-generated `thumb.avif`** | **Skip pre-generation; rely on edge cache from first `cf.image` request**.                                                         | `src/routes/api/r2/[...key]/+server.ts` (on-the-fly transform with `cf.image`); no write-path AVIF generation in `src/lib/server/jobs/create.ts`.                                                                           |

## Non-functional posture

| NFR                                                   | Status               | Notes                                                                                                                                                                                                                                                                                                  |
| ----------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **NFR-1 TTFR 10 images ≤ 8s**                         | deferred-measurement | Cannot honestly produce from local dev. Measure post-deploy against Workers + Workers AI.                                                                                                                                                                                                              |
| **NFR-2 50 images ≤ 120s**                            | deferred-measurement | Same rationale as NFR-1.                                                                                                                                                                                                                                                                               |
| **NFR-3 WebSocket p95 ≤ 500ms**                       | deferred-measurement | DO broadcast is implemented at `src/lib/server/durable-objects/job-coordinator.ts`; latency itself is a runtime metric.                                                                                                                                                                                |
| **NFR-4 LCP ≤ 1.5s on `/jobs` and `/leads`**          | deferred-measurement | Requires real-network measurement post-deploy.                                                                                                                                                                                                                                                         |
| **NFR-5 WCAG 2.2 AA**                                 | partial              | `aria-label` coverage on interactive primitives, semantic HTML throughout, keyboard nav via bits-ui. Formal audit not run.                                                                                                                                                                             |
| **NFR-6 Notion token encrypted at rest**              | implemented          | See Q4.                                                                                                                                                                                                                                                                                                |
| **NFR-7 Cloudflare Access in production**             | implemented          | See Q3. Dev fallback gated by `ALLOW_DEV_AUTH=1`.                                                                                                                                                                                                                                                      |
| **NFR-8 R2 not publicly addressable**                 | implemented          | All R2 reads go through `src/routes/api/r2/[...key]/+server.ts` which checks `locals.userId` and ownership before returning bytes.                                                                                                                                                                     |
| **NFR-9 Structured logs**                             | implemented          | Emit sites: `job_created` (create.ts:164), `item_queued` (create.ts:161), `item_started` (consumer.ts:310), `item_completed` (consumer.ts:509), `item_failed` (consumer.ts:325, 350, 405), `job_completed` (consumer.ts:624), `job_cancelled` (cancel.ts:48). Schema in `src/lib/server/analytics.ts`. |
| **NFR-10 AI errors carry r2_key**                     | implemented          | Commit `2a7e4b8`. All three `item_failed` emits in the consumer include `r2Key`; emit schema has a stable `blob6` slot.                                                                                                                                                                                |
| **NFR-11 Last-two-stable Chrome/Safari/Firefox/Edge** | deferred-measurement | No formal matrix. Will spot-check during F-phase Playwright verification.                                                                                                                                                                                                                              |
| **NFR-12 Safari iOS compatibility**                   | deferred-measurement | No platform-specific APIs in use.                                                                                                                                                                                                                                                                      |
| **NFR-13 Daily image quota**                          | implemented          | Enforced in `src/lib/server/jobs/create.ts` (default 1000/day per user, from `userSettings.dailyImageQuota`).                                                                                                                                                                                          |
