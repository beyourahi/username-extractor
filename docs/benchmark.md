# Kimi K2.6 accuracy benchmark

**Model:** `@cf/moonshotai/kimi-k2.6`
**Baseline:** `glm-ocr:bf16` (legacy Python CLI, Ollama)
**Generated:** 2026-05-24T11:36:07.624Z
**Sample size:** 58
**Source manifest:** `src/lib/extract/__tests__/fixtures/expected.json` (legacy extract_usernames needs_review.md @ 2026-02-13)

> PRD §Risks and §Final verification: the production app must beat or
> match the legacy `glm-ocr:bf16` baseline on a fixed evaluation set
> before launch. This report is the artifact of that protocol. It is
> **not** run in CI — every execution hits Workers AI and costs money.

## Summary

- **Status:** TEMPLATE — no live model run.
- **Reason:** ✘ [ERROR] Unknown arguments: remote, json, run, @cf/moonshotai/kimi-k2.6.
- **Total fixtures available:** 58
- **Matches:** TBD
- **Accuracy:** TBD
- **Baseline (`glm-ocr:bf16`):** ≈ 85% on this same needs-review set.
- **Delta vs baseline:** TBD

## Per-fixture results

| id | expected | got | match? | notes |
| --- | --- | --- | :---: | --- |
| IMG_4460 | `faphomoneaselo` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_3858 | `illustrationtshirtbd` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_4688 | `thre_adedtri_10` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_0377 | `emblaze_mb` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_1525 | `garb_bangladesh` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_3326 | `h10bd_` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_3091 | `bella_by_bristy` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_1876 | `511posts11.1kfollowersfo` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_2199 | `kablewala_bangladesh` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_5033 | `xavian.bd____` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_3292 | `theskinbar_bytasniaatique` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_2413 | `fashion._.world._.bd_by_alvina` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_4248 | `hierarchy_apparels` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_2189 | `get_hopes_clothing` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_0159 | `zed_burry_fragrance` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_4115 | `urbbansuftup` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_0398 | `rueofficial_bangladesh` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_3874 | `themattter434` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_0428 | `saltbistroandlcafl` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_2798 | `mis_lens` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_4464 | `hone_xp` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_3900 | `naaz_bangladesh` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_0548 | `enscented_50m` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_3218 | `fadedus_bd` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_2700 | `my_little_creationsz` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_4403 | `anrabess_official` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_1022 | `bamn_athleisure` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_0738 | `biucandles_333` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_3027 | `shomossha_nai` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_3151 | `nashitaalamanu` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_4950 | `herlan_bbangladesh` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_1347 | `atyourdoor0909` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_2840 | `feroze_bd` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_2471 | `bbagthatbxys` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_1344 | `pink_beautysalonbd` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_3802 | `bbreezysglobal` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_3340 | `felicite__clothing` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_3744 | `thelafz_bd` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_0679 | `tnk_the_nose_knows` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_3210 | `teenage_thrift_` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_4972 | `albeliz.com1` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_1613 | `clothya_bangladesh` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_0722 | `artsy____jewels` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_0939 | `theample_dhaka` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_1979 | `eleven____clothing` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_3766 | `fluid_dhaka` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_2297 | `sara___saira` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_0330 | `house103_official` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_0654 | `charlolttte` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_3273 | `armita_and_co` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_2390 | `cake_o_town` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_4324 | `nc_ink_tattoo_studio` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_3113 | `sesa_bangladesh` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_0751 | `loveworkostudlio` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_1665 | `starlin_books_bd` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_1066 | `beautycoma` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_4256 | `aura._.bd` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |
| IMG_0233 | `for_ever_bd` | `—` | ❌ | TBD — populate via `bun run benchmark` once wrangler ai run image input is wired |

## How to run

```bash
bun run benchmark
```

The script reads `src/lib/extract/__tests__/fixtures/expected.json`,
iterates each `(id, filename, expected)` triple, calls Workers AI via
`wrangler ai run @cf/moonshotai/kimi-k2.6 --remote` for every image,
normalizes the response with the production `cleanUsername` helper, and
writes a fresh `docs/benchmark.md`.

A baseline accuracy of **≈ 85%** for `glm-ocr:bf16` is the gate —
Kimi K2.6 must land within −2 pts of that figure (≥ 83%) to ship per the
PRD. Lower → reopen Open Question Q6 and either tune the prompt
(`src/lib/extract/prompt.ts`) or revisit model selection.

## How to extend the fixture set

The current 58 pairs come from the legacy `extract_usernames` Python
CLI's `needs_review.md` output (handles the human reviewer manually
confirmed). Source path on disk:

```
/Users/beyourahi/Desktop/extract usernames          /
├── needs_review.md
└── cropped_usernames_images/*_crop.avif
```

To extend:

1. Parse new `(IMG_id, expected_username)` pairs from `needs_review.md`
   (one entry per numbered block).
2. Convert each `IMG_XXXX_crop.avif` → PNG with macOS-native sips:

   ```bash
   sips -s format png input.avif --out output.png
   ```

3. Copy the PNG into `src/lib/extract/__tests__/fixtures/screenshots/`.
4. Append the new pair to the `pairs` array in
   `src/lib/extract/__tests__/fixtures/expected.json`.
5. Rerun `bun run benchmark`.

Hard upper bound is the legacy needs-review set size (~60 entries). To go
beyond, harvest additional confirmed handles from `verified_usernames.md`
in the same source directory.

## Why this isn't in CI

Workers AI calls cost money per token / per image. Running 58+ image
inferences on every push is wasteful and racy under quotas. The
benchmark is an opt-in pre-release check, not a regression gate. The
regression gate is the unit suite in `src/lib/extract/__tests__/`
(`cleanUsername`, confidence scoring, classification, Levenshtein near-
dup detection) — those run free, locally, in CI.
