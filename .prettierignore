# Auto-generated by scripts/benchmark.ts — formatting handled in the generator.
docs/benchmark.md

# Vendored Dropout Design System — synced verbatim from the DS repo via `bun run
# sync-ds`. Never hand-format it (a reformat would diverge from upstream and be
# reverted on the next sync). Its own repo owns its formatting.
src/lib/ds

# Generated Drizzle migrations — tool-generated snapshots/SQL; prettier reformatting
# them fights drizzle's output and dirties the tree on every format run.
migrations/
